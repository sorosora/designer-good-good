import styled from 'styled-components';
import { GridWrapper } from '@sorosora/grid';

const Wrapper = styled.div``;

const IndexPage = () => (
  <Wrapper>
    <GridWrapper>
      designer-good-good with Next.js
    </GridWrapper>
  </Wrapper>
);

export default IndexPage;
